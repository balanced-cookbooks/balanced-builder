# -*- mode: ruby -*-
chef_api :config
site :opscode

cookbook 'apt-transport-s3', path: '../apt-transport-s3'
cookbook 'balanced-user', path: '../balanced-user'
cookbook 'balanced-python', path: '../balanced-python'

metadata
